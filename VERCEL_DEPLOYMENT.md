# Деплой на Vercel - Инструкция

## Проблемы и решения

### Проблема 1: 404 на всех страницах кроме главной
**Причина**: Vercel не знает, как обрабатывать клиентские маршруты React Router

**Решение**: Создан `vercel.json` с правилом rewrite для всех путей на `index.html`

### Проблема 2: 404 на API запросы (/v1/auth/login)
**Причина**: Vercel не проксирует запросы к бэкенду автоматически

**Решение**: В `vercel.json` добавлен rewrite для `/v1/*` на ваш бэкенд

### Проблема 3: Картинки не загружаются
**Причина**: Неправильные пути к статическим файлам

**Решение**: Используйте относительные пути и храните файлы в `public/` или `src/assets/`

## Файлы конфигурации

### vercel.json
```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/v1/:path*",
      "destination": "http://78.153.139.47:8000/v1/:path*"
    },
    {
      "source": "/:path((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
```

**Что делает**:
- Первый rewrite: перенаправляет все API запросы `/v1/*` на ваш бэкенд
- Второй rewrite: все запросы без расширения файла отправляет на `index.html` (для React Router)

### .env.production
```
VITE_API_URL=
```

**Что делает**: Пустое значение заставляет использовать относительные пути (через Vercel rewrites)

### vite.config.js
Добавлены настройки:
```javascript
base: '/',
build: {
  outDir: 'dist',
  assetsDir: 'assets',
  sourcemap: false,
}
```

## Шаги для деплоя

### 1. Подготовка
```powershell
# Убедитесь, что все изменения закоммичены
git add .
git commit -m "Add Vercel configuration"
git push
```

### 2. Деплой на Vercel

#### Вариант A: Через Vercel Dashboard (рекомендуется)
1. Зайдите на https://vercel.com
2. Нажмите "Import Project"
3. Выберите ваш GitHub репозиторий
4. Vercel автоматически определит настройки из `vercel.json`
5. Нажмите "Deploy"

#### Вариант B: Через Vercel CLI
```powershell
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel --prod
```

### 3. Проверка
После деплоя проверьте:
- ✅ Главная страница загружается
- ✅ Переход на /login работает
- ✅ Переход на /register работает
- ✅ Вход в систему работает (API запросы проходят)
- ✅ Картинки загружаются

## Важные замечания

### CORS
⚠️ **Важно**: Ваш бэкенд должен разрешать запросы с домена Vercel!

Добавьте в бэкенд (FastAPI):
```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:5173",
        "https://piapav.vercel.app",  # ваш домен на Vercel
        "https://*.vercel.app"  # все preview деплои
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### Переменные окружения на Vercel
Если нужно изменить API URL в будущем:
1. Зайдите в настройки проекта на Vercel
2. Settings → Environment Variables
3. Добавьте `VITE_API_URL` с нужным значением
4. Пересоберите проект

### Картинки и статические файлы
- Храните в `public/` → доступны как `/filename.png`
- Или в `src/assets/` → импортируйте в компонентах
- Не используйте абсолютные пути типа `C:/...`

## Отладка проблем

### Если страницы всё ещё 404:
1. Проверьте, что `vercel.json` в корне проекта
2. Удалите проект на Vercel и создайте заново
3. Проверьте логи сборки в Vercel Dashboard

### Если API запросы не проходят:
1. Откройте DevTools → Network
2. Проверьте URL запроса (должен быть `/v1/...`)
3. Проверьте CORS на бэкенде
4. Убедитесь, что бэкенд доступен извне (не localhost)

### Если картинки не загружаются:
1. Проверьте путь к файлу
2. Убедитесь, что файл в `public/` или `src/assets/`
3. Проверьте регистр имени файла (в Linux чувствителен к регистру)

## Команды для локального тестирования production билда

```powershell
# Соберите production версию
npm run build

# Запустите preview
npm run preview

# Или с помощью serve
npm install -g serve
serve -s dist
```

Теперь откройте http://localhost:4173 (или порт, который покажет команда) и проверьте, что всё работает как в production.

## Автоматический деплой

После первого деплоя, каждый push в main ветку будет автоматически деплоиться на Vercel! 🚀

## Полезные ссылки

- Vercel Documentation: https://vercel.com/docs
- Vite Deploy Guide: https://vitejs.dev/guide/static-deploy.html
- React Router on Vercel: https://vercel.com/guides/deploying-react-with-vercel
