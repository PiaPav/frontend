# ✅ Чек-лист тестирования новой реализации gRPC

## Перед тестированием:

- ✅ Пакеты установлены: `google-protobuf`, `grpc-web`
- ✅ Proto файлы организованы: `backend-proto/proto/`
- ✅ JS файлы созданы: `src/grpc/*.js`
- ✅ Новый grpcClient активирован
- ✅ Резервная копия создана: `grpcClient-old.js`

## Запуск dev сервера:

```powershell
npm run dev
```

## Основные тесты:

### ✅ 1. Тест подключения к stream
- Откройте страницу ProjectAnalysis
- Проверьте консоль: `📤 ОТПРАВКА gRPC ЗАПРОСА (NEW)`
- Убедитесь нет CORS ошибок

### ✅ 2. Тест получения данных
- Дождитесь сообщений: START → REQUIREMENTS → ENDPOINTS → ARHITECTURE → DONE
- Проверьте что данные отображаются на странице

### ✅ 3. Тест визуализации
- Requirements показаны
- Endpoints в таблице
- Граф архитектуры построен

### ✅ 4. Тест ошибок
- Откройте несуществующий проект
- Проверьте читаемость сообщения об ошибке

## Откат при проблемах:

```powershell
Copy-Item "src\services\grpcClient-old.js" "src\services\grpcClient.js" -Force
```

## После успешного тестирования:

Удалите временные файлы:
```powershell
Remove-Item "src\services\grpcClient-old.js"
Remove-Item "src\services\grpcClient-new.js"
Remove-Item "src\services\grpcClient-grpc-web.js"
```
